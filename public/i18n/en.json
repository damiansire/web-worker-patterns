{
  "chrome": {
    "appTitle": "Web Worker Patterns"
  },
  "examples": {
    "01-setinterval-counter": {
      "title": "Counter with setInterval",
      "summary": "This example shows how to use setInterval to run code periodically. It's fundamental to understand before learning about Web Workers, since counters are a common way to demonstrate how the main thread can get blocked.",
      "whatToWatch": "Watch the two lanes: when the counter runs in a worker, the main thread stays free and the UI responds. When it runs on the main thread, that lane is blocked and the UI freezes.",
      "note": "This counter works perfectly because the main thread is free. When the main thread is busy with heavy work, the same counter freezes — which is exactly why you move it to a worker. The comparison above shows that difference.",
      "takeaways": {
        "title": "Key takeaways",
        "items": [
          "setInterval runs its callback on the main thread",
          "If the main thread is busy, the intervals are delayed",
          "This is the fundamental problem Web Workers solve"
        ],
        "tip": "Use requestAnimationFrame instead of setInterval for smoother animations."
      }
    },
    "03-basic-communication": {
      "title": "Basic communication",
      "summary": "The \"Hello World\" of Web Workers: you type a message and send it to the worker. The worker receives it, processes it, and replies back. That's how threads talk to each other — postMessage in both directions.",
      "whatToWatch": "→ leaves the main thread toward the worker, ← the processed reply comes back. The ms number is the round-trip: how long the whole journey took.",
      "note": "postMessage CLONES the data (structured clone): the worker gets a copy, not the reference. That's fine for small messages; for large data, prefer transferables (example 07).",
      "takeaways": {
        "title": "Key takeaways",
        "items": [
          "postMessage() sends data from main to worker and back again",
          "Data is automatically cloned (structured clone)",
          "onmessage receives messages on both sides",
          "Communication is async: the main thread doesn't block waiting for the reply"
        ],
        "tip": "Always handle the onerror event to catch silent worker failures."
      }
    },
    "04-offloading-computation": {
      "title": "Offload heavy work",
      "summary": "Here's the payoff of Web Workers: getting heavy work off the main thread. We count prime numbers — a deliberately intensive computation — and run it on both sides so you feel the difference.",
      "whatToWatch": "Run it in a worker: the live timer keeps ticking and you can still interact — the UI never janks. Run the SAME computation on the main thread: the whole page freezes (no repaint, no clicks) until it's done.",
      "note": "Any work that takes more than ~16ms blocks a frame and shows. A worker runs in true parallel on another thread: the computation takes the same time, but the main thread stays free to respond. The result comes back via postMessage (cloned).",
      "takeaways": {
        "title": "Key takeaways",
        "items": [
          "Heavy work on the main thread freezes the whole UI while it runs",
          "A worker runs in real parallel: the main thread stays responsive",
          "The computation isn't faster in the worker — but the UI doesn't jank",
          "The result returns via postMessage (structured clone); for large data, transferables (example 07)"
        ],
        "tip": "Rule of thumb: if a task can take longer than one frame (~16ms), send it to a worker."
      }
    },
    "05-error-handling": {
      "title": "Error handling",
      "summary": "An error inside a worker doesn't crash your page. The task here parses JSON: a valid payload returns its key count; a broken one makes JSON.parse throw a real SyntaxError. The worker lets that error propagate and the main thread catches it.",
      "whatToWatch": "Send a valid JSON (✓ the result is logged) and then a broken one (✗ the main thread catches the error via onerror). Notice the app stays responsive: you can keep running tasks after the error — the worker didn't die.",
      "note": "An uncaught error inside the worker fires the onerror event on the main thread's Worker object; it does NOT crash the page or kill the worker. For more control, catch the error INSIDE the worker (try/catch) and report it as a normal postMessage: that way you can type the error and add context.",
      "takeaways": {
        "title": "Key takeaways",
        "items": [
          "An uncaught error in the worker reaches the main thread as worker.onerror",
          "The page doesn't crash: the worker stays alive and you can run more tasks",
          "ErrorEvent carries message, filename and lineno to diagnose",
          "For more control, use try/catch inside the worker and report the error via postMessage"
        ],
        "tip": "Call event.preventDefault() in onerror to silence the red console log if you already surface the error in your UI."
      }
    },
    "06-lifecycle-termination": {
      "title": "Lifecycle and termination",
      "summary": "A worker doesn't live forever: you create it, it runs, and you terminate it. Here you start a long task that advances step by step and can cut it off midway. terminate() kills the worker instantly — the in-flight step is lost and that worker can't be reused.",
      "whatToWatch": "Start the task and watch the bar climb. Hit Terminate midway: progress freezes where it was and the worker stops existing. To run again you must create a new one (the Start button does that). If you let it finish, the worker closes itself with self.close().",
      "note": "terminate() is abrupt: no cleanup, no goodbye — the worker dies with its in-flight work (the pending setTimeout goes with it). A terminated worker can't be restarted: you make a new one. From the inside, a worker can close itself with self.close() once its work is done.",
      "takeaways": {
        "title": "Key takeaways",
        "items": [
          "worker.terminate() kills the worker instantly from the main thread",
          "In-flight work is discarded: no cleanup, no close event",
          "A terminated worker can't be reused: create a new one to run again",
          "self.close() lets the worker close itself when it's done"
        ],
        "tip": "Terminate workers you no longer use: they keep holding memory and a thread even while idle."
      }
    },
    "07-transferable-objects": {
      "title": "Transferable objects",
      "summary": "postMessage normally CLONES the data (structured clone). For large data — an ArrayBuffer — that costs. A Transferable is passed without copying (zero-copy): the buffer changes owner to the worker. The catch: the sender's buffer is left detached, empty and unusable.",
      "whatToWatch": "Send the same buffer both ways and compare the round-trip. Transferring is near-instant even for a big buffer; cloning takes longer because it copies byte by byte. And watch the side effect: after transferring, the main's buffer is DETACHED (0 bytes); when cloning, the main keeps its copy.",
      "note": "Transferring doesn't copy: it moves ownership of the buffer to the worker, which is why the sender's side is left detached (byteLength 0) and using it throws. Transferable types: ArrayBuffer, MessagePort, ImageBitmap, OffscreenCanvas, and ReadableStream/WritableStream. If you need the data on both sides, clone; if not, transfer and save the copy.",
      "takeaways": {
        "title": "Key takeaways",
        "items": [
          "postMessage clones by default; the 2nd argument (transfer list) transfers without copying",
          "Transferring is zero-copy: instant even for large buffers",
          "A transferred buffer is left detached on the sender's side (byteLength 0)",
          "Transferables: ArrayBuffer, MessagePort, ImageBitmap, OffscreenCanvas, streams"
        ],
        "tip": "If you won't touch the buffer again after sending it, transfer it: same data, without the cost of the copy."
      }
    }
  }
}
