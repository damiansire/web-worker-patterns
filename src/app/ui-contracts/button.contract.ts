/**
 * Contrato de botón (ARQUITECTURA §5): la forma que cada theme implementa con su propio
 * botón (`implements ButtonContract`). Da type-safety a los primitivos por theme.
 */
export abstract class ButtonContract {}
